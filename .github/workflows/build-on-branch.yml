name: 'Build'

on:
  pull_request: {}

jobs:
  check:
    uses: zenhelix/ci-workflows/.github/workflows/gradle-plugin-check.yml@v2
    with:
      java-version: '17'

  label:
    uses: zenhelix/ci-workflows/.github/workflows/labeler.yml@v2
